if(true){
    //const não pode ser inicializada sem atribuir um valor
    const a = undefined; //o valor inicial não poderá ser alterado ao decorrer do programa
    console.log(a); //uma const não funciona fora do bloco que foi criada
}